[project]
name = "ansible-ec2-ssm-deploy"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ansible-navigator>=25.1.0",
    "boto3>=1.37.1",
    "botocore>=1.37.1",
]
